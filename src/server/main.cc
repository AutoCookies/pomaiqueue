#include <iostream>

int main() {
  std::cout << "pomaiqueued is not built with gRPC in this configuration.\n";
  std::cout << "TODO: link gRPC server implementation to pomai_queue_core.\n";
  return 0;
}
